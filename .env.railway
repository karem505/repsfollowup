# Railway Production Environment Variables
# This file shows the environment variables needed for Railway deployment
# DO NOT commit actual secrets - Railway will auto-populate these values

# Backend Configuration
NODE_ENV=production
PORT=5000

# Database (Railway will auto-populate this from your Postgres service)
DATABASE_URL=${{Postgres.DATABASE_URL}}

# CORS - Set to your Railway domain
CLIENT_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}

# Security
JWT_SECRET=your-super-secret-random-jwt-key-change-this
POSTGRES_PASSWORD=${{Postgres.PGPASSWORD}}

# Frontend API URL - CRITICAL: Must be set before build!
REACT_APP_API_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}/api

# Google Maps (optional - add your real key)
GOOGLE_MAPS_API_KEY=your-google-maps-api-key
